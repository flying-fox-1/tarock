# Tarock
A simple tarock game made in python

## To install,
head to the releases page and download the desired release. 

## To run the game,
open powershell, cd into the folder where you have downloaded it (usually Downloads) `cd Downloads`, and run the game with `python simulation_game.py` or `python en_simulation_game.py` depending on the downloaded version
